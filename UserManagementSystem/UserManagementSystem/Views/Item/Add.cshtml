@using Microsoft.AspNetCore.Mvc.ModelBinding @*For strongly typed error variable*@
<!--
   FILE : Item/Add.cshtml
   PROGRAMMER : Name(s): Josiah Williams,Jeff, Gao Ricardo
   DESCRIPTION :this is for the /item/add

-->
@model UserManagementSystem.Models.AddViewModel

@{
    ViewData["Title"] = "Add Item";
}

<h2>Add Item</h2>

<form asp-controller="Item" asp-action="Add" method="post">
    @Html.AntiForgeryToken()
    <div style="margin-bottom:12px;">
        <label for="ItemName">Item Name:</label>
        <input type="text" id="ItemName" name="ItemName" />
    </div>

    <div style="margin-bottom:12px;">
        <label for="Description">Description:</label>
        <input type="text" id="Description" name="Description" />
    </div>

    <div style="margin-bottom:12px;">
        <label for="Quantity">Quantity:</label>
        <input type="number" id="Quantity" name="Quantity"/>
    </div>

    <button type="submit"
            style="width:110px; padding:8px; border:1px solid #007bff; background:#007bff; color:white; cursor:pointer; text-align:center;">
        Add Item
    </button>

    <a asp-controller="Item" asp-action="Index"
       style="width:110px; padding:8px; border:1px solid #007bff; background:#007bff; color:white; text-decoration:none; display:inline-block; text-align:center; margin-left:10px;">
        Back
    </a>
    <br />

    @if (!ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger">
            @foreach (ModelError error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
            {
                <p>@error.ErrorMessage</p>
            }
        </div>
    }
</form>